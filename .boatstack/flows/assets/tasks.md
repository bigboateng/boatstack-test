# Tasks
Emit deterministic task objects in dependency order. Each task must name its owner, inputs, outputs, verifier, dependencies, and termination condition and must implement an accepted specification requirement.
