# Allowed autonomy

The delegated agent may make the smallest implementation and test changes needed to add the visible-results count, using existing component structure and styling.

It may:
- Render the filtered result count from the existing visible task collection.
- Reuse existing count styling and accessibility conventions.
- Add focused component tests for the requested states and sorting invariant.
- Run configured formatting, build, lint, and test commands.
- Preserve and commit the planning package and verification artifacts.
- Rewrite the feature branch before publication to produce exactly one commit relative to `test/software-delivery-flow-dx-search-20260817`, then rerun all required checks and regenerate evidence for the final commit.
- Publish a real pull request and leave it open.

It may not:
- Change the meaning of the existing total count.
- Alter search, incomplete filtering, due-date sorting, selection, or focus behavior.
- Add unrelated design or application changes.
- Merge or deploy the pull request.
