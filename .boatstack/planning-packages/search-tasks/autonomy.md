# Allowed autonomy

The authorized agent may make bounded implementation and test changes within the basic React project, run its configured checks, repair attributable failures, commit and push the managed feature branch, and open the requested pull request. It must preserve the specified behavior, avoid unrelated refactors, leave both pull requests open, and never merge.
